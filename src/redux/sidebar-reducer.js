const sidebarReducer = (state, action) => {
    /*switch (action.type) {
        case blablabla:
            return state;
        default:
            return state;
    }*/

    return state;
};

export default  sidebarReducer;